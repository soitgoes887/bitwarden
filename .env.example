# Domain Configuration
# IMPORTANT: You need a domain name for HTTPS to work properly
# Option 1: Use a free DDNS service (recommended):
#   - DuckDNS: https://www.duckdns.org (e.g., https://yourusername.duckdns.org)
#   - No-IP: https://www.noip.com
# Option 2: Use your own domain and point it to 81.96.238.148
#
# Set your domain here (with https://):
DOMAIN=https://vault.example.com

# Server Ports (don't change these unless you have a reason)
# Caddy will handle ports 80/443 externally
HTTP_PORT=80
WEBSOCKET_PORT=3012

# Registration & Invitations
SIGNUPS_ALLOWED=true
INVITATIONS_ALLOWED=true
SHOW_PASSWORD_HINT=false

# Admin Panel Configuration
# Generate a secure token with: openssl rand -base64 48
ADMIN_TOKEN=your-secure-admin-token-here

# Security Settings
WEBSOCKET_ENABLED=true
WEB_VAULT_ENABLED=true

# SMTP Configuration (Optional - for email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_FROM=bitwarden@example.com
# SMTP_PORT=587
# SMTP_SECURITY=starttls
# SMTP_USERNAME=your-email@example.com
# SMTP_PASSWORD=your-app-password

# PostgreSQL Database Configuration (Optional - uses SQLite by default)
# DB_USER=bitwarden
# DB_PASSWORD=your-secure-database-password
# DB_NAME=bitwarden

# AWS S3 Backup Configuration (Required for automated backups)
# Get these from AWS IAM: https://console.aws.amazon.com/iam/
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=eu-west-2
S3_BUCKET=your-bucket-name
S3_PREFIX=bitwarden/

# Optional: For S3-compatible services (Wasabi, MinIO, DigitalOcean Spaces, etc.)
# AWS_ENDPOINT=https://s3.wasabisys.com

# Backup Retention Settings
KEEP_LOCAL_BACKUPS=7
DELETE_LOCAL_AFTER_UPLOAD=false

# Timezone (for backup scheduling)
TZ=Europe/London
